import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Latency Topology Visualizer",
  description: "Real-time 3D visualization of cryptocurrency exchange server locations and latency data across AWS, GCP, and Azure regions",
  keywords: ["cryptocurrency", "trading", "latency", "3D visualization", "exchange servers", "cloud infrastructure"],
  authors: [{ name: "Latency Topology Visualizer" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
